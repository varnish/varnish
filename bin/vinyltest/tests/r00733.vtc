vtest "HTTP/1.1 Backend sends no length hint"

server s1 {
	rxreq
	send "HTTP/1.0 200 OK\n"
	send "\n"
	send "12345"
} -start

vinyl v1 -vcl+backend {} -start

client c1 {
	txreq
	rxresp
	expect resp.bodylen == 5
} -run
