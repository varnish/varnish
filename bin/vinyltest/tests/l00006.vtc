vtest "Check shmlog stats"

server s1 {
    rxreq
    txresp
} -start

vinyl v1 -vcl+backend "" -start

client c1 {
    txreq
    rxresp
} -run

vinyl v1 -vsl_catchup
vinyl v1 -expect shm_writes > 0
vinyl v1 -expect shm_records > 0
vinyl v1 -expect shm_bytes > 0
