vtest "#1275 - panic with malformed Vary header"

server s1 {
       rxreq
       txresp -hdr "Vary: foo bar"
} -start

vinyl v1 -vcl+backend { } -start

client c1 {
       txreq
       rxresp
       expect resp.status == 200
} -run
