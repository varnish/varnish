vtest "Junk request should not go to vcl_synth"

server s1 {
	rxreq
	txresp
} -start

vinyl v1 -vcl+backend {
	sub vcl_synth {
		return (restart);
	}
} -start

client c1 {
	send "sljdslf\r\n\r\n"
	delay .1
} -run

client c1 {
	txreq
	rxresp
} -run
